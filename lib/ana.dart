import 'package:flutter/material.dart';

class Ana extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    appBar: AppBar(title:Text("Analyse Yourself"),
    backgroundColor: Colors.blueAccent,
     ),
    );
  }
}